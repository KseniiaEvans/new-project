# New Project

1. Clone repository: 
```bash
git clone https://github.com/KseniiaEvans/new-project.git

cd new-project
```

2. Create new branch for development
git checkout -b feature/<feature-name>

3. Commit and push the changes:
```bash
git add .
git commit -m "Commit Message"
git push
```
